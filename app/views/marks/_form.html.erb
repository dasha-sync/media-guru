<%= form_for [@film, @review] do |f| %>
  <!-- Your form fields for the review -->
  <div class="field">
    <%= f.text_area :text, rows: 1, class: "form-control", placeholder: 'Написать рецензию' %>
  </div>
  <%= f.submit 'Опубликовать', class: "btn btn-primary" %>
  <br><br>
<% end %>
